<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                <div class="card-header">
                    <h3>Restablecer Contraseña</h3>
                </div>
                <div class="card-body">
                    <form action="/resetpassword/{{token}}" method="POST">
                        <!-- Campo oculto para el correo electrónico -->
                        <input type="hidden" name="email" value="{{email}}">
                        <div class="form-group">
                            <div class="input-group">
                                <input type="password" name="password" id="password" placeholder="Ingrese una contraseña" class="form-control" autofocus required>
                                <div class="input-group-append">
                                    <span class="input-group-text" id="toggle-password">
                                        <i class="fa fa-eye-slash" id="eye-icon"></i> <!-- El ícono de ojo (oculto por defecto) -->
                                    </span>
                                </div>
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="input-group">
                                <input type="password" name="confirmPassword" id="confirmPassword" placeholder="Confirma la contraseña" class="form-control" autofocus required>
                                <div class="input-group-append">
                                    <span class="input-group-text" id="toggle-confirm-password">
                                        <i class="fa fa-eye-slash" id="confirm-eye-icon"></i> <!-- El ícono de ojo (oculto por defecto) -->
                                    </span>
                                </div>
                            </div>
                        </div>
                        <div class="form-group">
                            <button class="btn btn-success btn-block">Restablecer Contraseña</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
<script>
    // Obtener los inputs y los iconos de ojo
    const passwordInput = document.getElementById('password');
    const confirmPasswordInput = document.getElementById('confirmPassword');
    const eyeIcon = document.getElementById('eye-icon');
    const confirmEyeIcon = document.getElementById('confirm-eye-icon');
    const togglePassword = document.getElementById('toggle-password');
    const toggleConfirmPassword = document.getElementById('toggle-confirm-password');

    // Función para mostrar/ocultar la contraseña
    function toggleVisibility(input, icon) {
        if (input.type === 'password') {
            input.type = 'text';  // Cambiar tipo a texto para mostrar la contraseña
            icon.classList.remove('fa-eye-slash');  // Quitar ícono de ojo cerrado
            icon.classList.add('fa-eye');  // Mostrar ícono de ojo abierto
        } else {
            input.type = 'password';  // Cambiar tipo a contraseña para ocultarla
            icon.classList.remove('fa-eye');  // Quitar ícono de ojo abierto
            icon.classList.add('fa-eye-slash');  // Mostrar ícono de ojo cerrado
        }
    }

    // Evento para mostrar/ocultar la contraseña
    togglePassword.addEventListener('click', function() {
        toggleVisibility(passwordInput, eyeIcon);
    });

    // Evento para mostrar/ocultar la confirmación de contraseña
    toggleConfirmPassword.addEventListener('click', function() {
        toggleVisibility(confirmPasswordInput, confirmEyeIcon);
    });
</script>